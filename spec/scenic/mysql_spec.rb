require "spec_helper"

describe Scenic::Mysql do
  it "has a version number" do
    expect(Scenic::Mysql::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
